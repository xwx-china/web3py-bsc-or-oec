# # 所有权转移（地址，地址）
# OwnershipTransferred(address,address)
# # 添加Minter（地址）
# addMinter(address)
# # 德尔明特（地址）
# delMinter(address)
# # 宝石上次（地址）
# gemLastTime(address)
# # 获得所有奖励（uint256，地址）
# getAllReward(uint256,address)
# # 获得宝石奖励（地址）
# getGemProdBonus(address)
# # get Minter（uint256）
# getMinter(uint256)
# # 获得店铺促销奖励（地址）
# getStoreProdBonus(address)
# # 获得树戳奖励（地址）
# getTreeProdBonus(address)
# # 获取用户最后时间（地址）
# getUserLastTime(address)
# # 初始用户时间（地址）
# initUserTime(address)
# # 内部存款资源（地址）
# innerDepositResource(address)
# # 内部提取资源（地址，地址，uint256）
# innerWithdrawResource(address,address,uint256)
# # 是明特（地址）
# isMinter(address)
# # 我是最后一次（地址）
# islLastTime(address)
# # 最后一次（地址）
# lastDefTime(address)
# # 设置地址（地址，地址，地址，地址，地址，地址，地址，地址，地址）
# setAddr(address,address,address,address,address,address,address)
# # 斯通最后一次（地址）
# stoneLastTime(address)
# # 上次（地址）
# timberLastTime(address)
# # 转让所有权（地址）
# transferOwnership(address)
# # 提取资源（uint256）
# withdrawResource(uint256)





















